#include <iostream>

using namespace std;

int main()
{

    //use double instead of float because double has high precision.
    //two data types: real and imaginary

    bool value{}; //{} initializes the value to 0 (empty)
    if (value) {
        cout << "This will not happen." << endl;
    }
    value = true;
    if (value) {
        cout << "This will happen." << endl;
    }
    else {
        cout <<
    }

    return 0;
}